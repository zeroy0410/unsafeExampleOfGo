1. 统一抽象语法树中间表达（Unified AST / UAST）标准定制，UAST标准提供统一语言中间表
示，作为语言和分析引擎衔接层，提供归一化的语法节点描述，提供语言解析套件输出格式校验，
其中包含不同类型的优化器，实现分析前优化等能力。
2. 基于UAST的数据流分析引擎（yet another static analyzer / YASA）建设，YASA引擎负责
引擎算法执行，以UAST格式数据作为输入，经过核心分析算法模块，生成对应控制流，数据流依
赖，并且在过程中，引入规则模块，进行具体规则检查。如果有规则匹配，则通过上报模块进行
问题封装及上报。
3. 查询式语言（YAQL），查询式规则语言，通过声明式的规则描述，帮助业务和安全人员快速
定制规则，结合统一语言表达方式及底层数据流引擎，帮助使用者通过程序分析底座快速查找和
定位问题。

UAST标准规范具体是什么规范，有没有相关的文档提供。是要把某个语言当前已有的AST基础设施生成的AST转换为UAST标准的意思吗。

YAQL查询式语言有相关文档提供吗？具体需要实现什么功能，能否举例说明。YASA引擎有现有实现和接口规范吗，还是也需要自己实现。

当前系统已有哪些能力，已有的能力能否提供接口规范。

宏观的完整的系统的使用场景能否举个具体的例子说明。
